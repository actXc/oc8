# Dummy secrets for `helm template`, `helm lint`, and CI only — never use in production.
secrets:
  jwtSecret: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  secretKek: "ZHVtbXktc2VjcmV0LWtlay1mb3ItY2ktb25seQ=="
  postgresPassword: "ci-postgres-password"

oc8:
  seedOnStart: false

caddy:
  service:
    type: ClusterIP
