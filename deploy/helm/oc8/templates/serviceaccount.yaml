apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oc8.serviceAccountName" . }}
  labels:
    {{- include "oc8.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
