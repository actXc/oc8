"""The unified credentials framework (design doc: unified credentials
framework, 2026-08-21). `service.py` is the CRUD + test surface over the
`credential` table; `registry.py` is the credential_type catalog (core +
Capa-contributed)."""
