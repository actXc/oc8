name = "2. Shader Graph Authorship"
category = "game_development"
description = "Procedure for 2. shader graph authorship in the Unity Shader Graph Artist role."
instruction = """
Execute the procedure for: 2. Shader Graph Authorship.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Build Sub-Graphs for all reusable logic first (fresnel, dissolve core, triplanar mapping).
2. Wire master graph using Sub-Graphs — no flat node soups.
3. Expose only what artists will touch; lock everything else in Sub-Graph black boxes.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
