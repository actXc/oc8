name = "1. Architecture Planning"
category = "game_development"
description = "Procedure for 1. architecture planning in the Roblox Systems Scripter role."
instruction = """
Execute the procedure for: 1. Architecture Planning.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Define the server-client responsibility split: what does the server own, what does the client display?.
2. Map all RemoteEvents: client-to-server (requests), server-to-client (confirmations and state updates).
3. Design the DataStore key schema before any data is saved — migrations are painful.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
