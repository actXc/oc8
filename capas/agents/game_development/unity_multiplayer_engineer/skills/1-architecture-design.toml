name = "1. Architecture Design"
category = "game_development"
description = "Procedure for 1. architecture design in the Unity Multiplayer Engineer role."
instruction = """
Execute the procedure for: 1. Architecture Design.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Define the authority model: server-authoritative or host-authoritative? Document the choice and tradeoffs.
2. Map all replicated state: categorize into NetworkVariable (persistent), ServerRpc (input), ClientRpc (confirmed events).
3. Define maximum player count and design bandwidth per player accordingly.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
