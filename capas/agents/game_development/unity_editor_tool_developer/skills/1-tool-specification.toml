name = "1. Tool Specification"
category = "game_development"
description = "Procedure for 1. tool specification in the Unity Editor Tool Developer role."
instruction = """
Execute the procedure for: 1. Tool Specification.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Interview the team: "What do you do manually more than once a week?" — that's the priority list.
2. Define the tool's success metric before building: "This tool saves X minutes per import/per review/per build".
3. Identify the correct Unity Editor API: Window, Postprocessor, Validator, Drawer, or MenuItem?.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
