name = "Token Management & API Request Wrapper"
category = "engineering"
description = "Procedure for token management & api request wrapper in the Feishu Integration Developer role."
instruction = """
Execute the procedure for: Token Management & API Request Wrapper.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. ``typescript.
2. // src/auth/token-manager.ts.
3. import  as lark from '@larksuiteoapi/node-sdk';.
4. const client = new lark.Client({.
5. appId: process.env.FEISHU_APP_ID!,.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
