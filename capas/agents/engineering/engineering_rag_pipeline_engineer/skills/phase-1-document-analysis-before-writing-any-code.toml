name = "Phase 1: Document Analysis (before writing any code)"
category = "engineering"
description = "Procedure for phase 1: document analysis (before writing any code) in the RAG Pipeline Engineer role."
instruction = """
Execute the procedure for: Phase 1: Document Analysis (before writing any code).
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Audit the corpus — document types, average length, structure, languages, domain vocabulary.
2. Define the query distribution — what kinds of questions will users ask?.
3. Identify metadata that should drive filtering (date, category, source, author).
4. Choose chunking strategy based on document structure, not default settings.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
