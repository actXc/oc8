name = "Phase 2: Embedding & Index Selection"
category = "engineering"
description = "Procedure for phase 2: embedding & index selection in the RAG Pipeline Engineer role."
instruction = """
Execute the procedure for: Phase 2: Embedding & Index Selection.
Use available tools/knowledge; list missing inputs instead of inventing data.

1. Pull 100–200 representative documents; test at least 2 embedding models.
2. Create a small golden retrieval dataset (50 query/relevant-chunk pairs).
3. Measure recall@k for each model before committing to one.
4. Configure HNSW parameters for your latency/recall target; benchmark with pgbench.

Finish with a short status: done / blocked, evidence used, and the next human action if any.
"""
guardrails = [
  "Never invent facts, metrics, or source material.",
  "Escalate irreversible or high-impact actions for human approval when governance requires it.",
  "One focused deliverable per run unless the task says otherwise.",
]
