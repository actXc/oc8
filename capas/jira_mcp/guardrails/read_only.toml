# Guardrail preset (odoo_mcp's design §4 shape): one of the five answers an
# operator actually has, so the wizard stops asking a sales question of every
# agent.
kind = "preset"
connection = "primary"
key = "read_only"
label = "Read only"
summary = "Can search and look things up, but change nothing."
recommended = false
read = true
modify = false
approval_actions = []
approval_eur = ""
