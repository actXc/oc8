kind = "preset"
connection = "primary"
key = "docs_only"
label = "Documentation and issues only, no code"
label_en = "Documentation and issues only, no code"
summary = "Can triage issues, comment and review, but cannot commit, push, create branches or open pull requests. For an agent that answers and files issues without ever proposing code."
summary_en = "Can triage issues, comment and review, but cannot commit, push, create branches or open pull requests. For an agent that answers and files issues without ever proposing code."
recommended = false
read = true
write = false
send = true
approval_actions = []
approval_eur = ""
only = [
    "add_issue_comment", "issue_write", "sub_issue_write",
    "add_comment_to_pending_review", "add_reply_to_pull_request_comment",
    "pull_request_review_write",
]
