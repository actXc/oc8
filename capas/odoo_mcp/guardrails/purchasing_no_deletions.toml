# Curated use-case library entry (design doc §7) -- one of odoo_mcp's 28
# documented ERP scenarios, written as documentation by example for anyone
# building an oc8 tool-pack plugin. Every entry is built from the SAME 9
# generic tools (tool_pack.toml's connection scopes); none of them can restrict
# a call to one Odoo model, so entries differ in prose and in the specific
# approval_eur/approval_actions/only chosen, not in a deeper technical
# restriction. No tool is classified `write`, so every `write` here is false.
# Each summary says what it grants AND what it deliberately does not cover.

kind = "library"
key = "purchasing_no_deletions"
label = "Einkauf bearbeiten, nie löschen"
label_en = "Work purchasing, never delete"
summary = "Legt Bestellungen und Anfragen an und bearbeitet sie; delete_record steht nicht zur Verfügung. Deckt keine Stornierung ab, die als Statusänderung statt als Löschung modelliert ist -- das bleibt weiterhin möglich. Deckt auch keine Bereichsgrenze ab: das Ausschließen von delete_record gilt für jedes Odoo-Modell, das die Verbindung erreicht, nicht nur für den Einkauf."
summary_en = "Creates and updates purchase orders and requisitions; delete_record is withheld. Does not cover a cancellation modelled as a status change instead of a deletion -- that remains possible. Also does not cover a domain boundary: excluding delete_record applies to any Odoo model the connection reaches, not only purchasing."
use_case = "purchasing"
read = true
write = false
send = true
approval_eur = ""
approval_actions = []
only = ["search_records", "get_record", "list_models", "list_resource_templates", "aggregate_records", "create_record", "update_record", "post_message"]
