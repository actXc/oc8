# Curated use-case library entry (design doc §7) -- one of odoo_mcp's 28
# documented ERP scenarios, written as documentation by example for anyone
# building an oc8 tool-pack plugin. Every entry is built from the SAME 9
# generic tools (tool_pack.toml's connection scopes); none of them can restrict
# a call to one Odoo model, so entries differ in prose and in the specific
# approval_eur/approval_actions/only chosen, not in a deeper technical
# restriction. No tool is classified `write`, so every `write` here is false.
# Each summary says what it grants AND what it deliberately does not cover.

kind = "library"
key = "purchasing_read_only"
label = "Einkauf, nur lesen"
label_en = "Purchasing, read only"
summary = "Darf Bestellungen, Anfragen und Lieferanten durchsuchen, aber nichts anlegen oder ändern. Deckt keinen Preisvergleich gegen externe Kataloge ab. Deckt auch keine Bereichsgrenze ab: read=true gilt für jedes Odoo-Modell, das die Verbindung erreicht, nicht nur für Einkaufsdaten."
summary_en = "Can search purchase orders, requisitions and suppliers, but create or change nothing. Does not cover a price comparison against external catalogues. Also does not cover a domain boundary: read=true applies to any Odoo model the connection reaches, not only purchasing data."
use_case = "purchasing"
read = true
write = false
send = false
approval_eur = ""
approval_actions = []
only = []
