kind = "library"
key = "full_assistant_with_send_approval"
label = "Full assistant with send approval"
summary = "Every category is reachable; every action that reaches a person (sending and replying to mail) needs approval. Teams is read-only here — Microsoft Graph does not allow sending Teams messages without a signed-in person. Does not cover a monetary threshold — no tool in this pack carries an amount."
use_case = "general"
read = true
modify = true
approval_actions = ["modify"]
approval_eur = ""
only = []
