kind = "library"
key = "mail_and_calendar_only"
label = "Mail and Calendar only"
summary = "Restricted to Outlook Mail and Calendar — Files, Teams, Word/Excel/PowerPoint are unreachable. Does not cover prioritizing between conflicting appointments; the tool only creates, it doesn't decide."
use_case = "mail"
read = true
modify = true
approval_actions = ["modify"]
approval_eur = ""
only = [
    "mail_search", "mail_get", "mail_list_folders", "mail_send", "mail_reply", "mail_create_draft",
    "mail_move", "mail_delete", "mail_mark_read",
    "calendar_list_events", "calendar_get_event", "calendar_create_event", "calendar_update_event",
    "calendar_cancel_event", "calendar_respond_to_invite", "calendar_check_free_busy",
]
