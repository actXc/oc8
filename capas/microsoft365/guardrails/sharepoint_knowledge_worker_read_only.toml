kind = "library"
key = "sharepoint_knowledge_worker_read_only"
label = "SharePoint knowledge worker, read only"
summary = "Can read and search files in SharePoint/OneDrive, but change nothing, send nothing, and act in no other category."
use_case = "files"
read = true
write = false
send = false
approval_actions = []
approval_eur = ""
only = ["files_list", "files_get_content", "files_search", "sites_list"]
