kind = "preset"
connection = "primary"
key = "no_schema_changes"
label = "Records only, no schema changes"
summary = "Creates, updates and associates records and engagements on its own, without approval -- but can never create or update a custom property. For a team that trusts an agent with day-to-day CRM work but not with the object schema everyone else relies on."
recommended = false
read = true
write = false
send = true
approval_actions = []
approval_eur = ""
only = [
    "hubspot-batch-create-objects", "hubspot-batch-update-objects",
    "hubspot-batch-create-associations", "hubspot-create-engagement",
    "hubspot-update-engagement",
]
