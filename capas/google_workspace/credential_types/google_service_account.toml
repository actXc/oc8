name = "google_service_account"
display_name = "Google service account"

[[fields]]
key = "service_account_key"
label = "Service account key (JSON)"
kind = "password"
help = "Paste the full contents of the service account's downloaded JSON key file."
