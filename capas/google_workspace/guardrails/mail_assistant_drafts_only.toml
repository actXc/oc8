kind = "library"
key = "mail_assistant_drafts_only"
label = "Mail drafts only"
summary = "Can read email and create drafts, but never actually send anything — gmail_send and gmail_reply are unreachable entirely."
use_case = "mail"
read = true
write = false
send = true
approval_actions = []
approval_eur = ""
only = ["gmail_search", "gmail_get", "gmail_list_labels", "gmail_create_draft", "gmail_modify_labels"]
