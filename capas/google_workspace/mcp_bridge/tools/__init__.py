"""One module per Google Workspace tool category. `__main__.py` aggregates each
module's `TOOLS`/`CALL_HANDLERS` -- adding a category is one new module here
plus one import and one dict-merge line there."""
